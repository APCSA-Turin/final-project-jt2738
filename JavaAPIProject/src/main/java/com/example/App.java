package com.example;
import java.util.Scanner;

public class App extends Api {
    public App (){
        //Ask if possible: generate a random number in the range of the number of indexes of characters that are in the array, and then call their specific api that way 
    }
}
